# 🔧 FIX - nodemon не найден

## ⚠️ ПРОБЛЕМА: Node.js НЕ УСТАНОВЛЕН

Диагностика показала, что на вашей системе **не установлены Node.js и npm**.

Команды `node --version` и `npm --version` не найдены.

## ✅ РЕШЕНИЕ: Установить Node.js

### Вариант 1: Установить Node.js (Рекомендуется)

1. Скачайте Node.js 16+ с https://nodejs.org/
2. Установите (выберите "Add to PATH")
3. Перезагрузитесь или откройте новый PowerShell
4. Проверьте:
```bash
node --version
npm --version
```

5. Затем установите зависимости:
```bash
cd backend
npm install
npm run dev
```

### Вариант 2: Использовать npx (Без глобальной установки)

Если у вас уже установлен Node.js, но nodemon не найден:

```bash
cd backend

# Установить зависимости
npm install

# Запустить с npx
npx nodemon src/server.js
```

### Вариант 3: Изменить package.json

Если хотите использовать встроенный скрипт:

```bash
cd backend
npm install
npm run dev
```

---

## ✅ Полная Инструкция Установки

### 1. Установить Node.js

**Скачать:**
- Зайти на https://nodejs.org/
- Скачать LTS версию (16+)
- Установить с default settings
- **Важно:** Выбрать "Add to PATH"

**Проверить установку:**
```bash
node --version  # должен показать версию, например v18.17.0
npm --version   # должен показать версию, например 8.19.4
```

### 2. Перейти в папку backend

```bash
cd d:\project\three\backend
```

### 3. Установить зависимости

```bash
npm install
```

Это установит:
- express
- nodemon
- все остальные пакеты

### 4. Запустить сервер

```bash
npm run dev
```

**Результат:**
```
🚀 Server is running!
📍 Listening on port 5000
```

**Проверить:**
Откройте в браузере: http://localhost:5000/health

---

## 🐛 Если ошибка Сохраняется

### 1. Очистить кэш npm

```bash
npm cache clean --force
```

### 2. Удалить папку node_modules

```bash
rm -r node_modules
```

Или на Windows:
```bash
rmdir /s /q node_modules
```

### 3. Переустановить

```bash
npm install
```

### 4. Запустить

```bash
npm run dev
```

---

## 📋 Проверка Окружения

```bash
# Проверить Node.js
where node
where npm

# Проверить версии
node -v
npm -v

# Проверить глобальные пакеты
npm list -g

# Проверить локальные пакеты
npm list
```

---

## 🔗 Полезные Ссылки

- Node.js Download: https://nodejs.org/
- npm Documentation: https://docs.npmjs.com/
- Nodemon Documentation: https://nodemon.io/

---

## 💡 Советы

1. **Используйте LTS версию Node.js** - Более стабильная
2. **Добавьте Node.js в PATH** - При установке выберите эту опцию
3. **Перезагрузитесь после установки** - Иногда помогает
4. **Используйте новый PowerShell** - После установки откройте новое окно

---

Если проблема не решена, проверьте [SETUP_GUIDE.md](./SETUP_GUIDE.md)
