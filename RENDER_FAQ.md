# ❓ FAQ и Решение Проблем Render.com

## ❓ Часто Задаваемые Вопросы

### Q: Сколько времени займет настройка?
**A:** 5-10 минут максимум:
- 2 минуты на создание БД
- 2-3 минуты на инициализацию БД
- 1-2 минуты на добавление переменных
- 1 минута на перезагрузку сервиса

---

### Q: Какой план выбрать для БД?
**A:** 
- **Free план** - хорошо для тестирования и небольших объемов
- **Starter план** - рекомендуется для production с реальными пользователями
- **Starter Plus и выше** - для масштабирования

На Free плане:
- 200 MB хранилища
- 500 Мб/месяц входящего трафика
- Достаточно для тестирования

---

### Q: Сайт не открывается вообще
**A:** Проверь:
1. Правильно ли ты установил все переменные окружения?
2. Нажал ли "Save Changes" в Render?
3. Дождался ли перезагрузки сервиса (могут быть спирали загрузки)?
4. Посмотри логи - там должны быть ошибки

Если логи говорят "ECONNREFUSED" - проблема в БД. Смотри раздел [БД не подключается](#бд-не-подключается)

---

### Q: Backend запустился, но данные не сохраняются
**A:**
1. **Проверь логи** - ищи фразу "Database models synchronized"
2. Если её нет - БД не подключилась
3. Если есть - может быть проблема с миграциями

Попробуй:
```bash
1. Смотри полные логи (может потребоваться прокрутка)
2. Ищи сообщения об ошибках
3. Проверь что DB_PASSWORD не содержит специальных символов
```

---

### Q: Сколько стоит использование?
**A:**
- **Free** - полностью бесплатно (но с ограничениями)
- **Starter** - от $7/месяц за backend + $15/месяц за БД
- Можешь начать с Free и перейти на платные планы позже

---

### Q: Где хранятся мои фото/данные?
**A:** 
- **Фото** - на сервере в папке `uploads/` (если используется локальное хранилище)
- **Данные пользователей** - в PostgreSQL БД на Render
- **Всё зашифровано** и передается по HTTPS

---

### Q: Как добавить собственный домен?
**A:**
1. На странице backend сервиса нажми "Settings"
2. Перейди на "Custom Domains"
3. Добавь свой домен
4. Обнови DNS записи в своем провайдере домена

Подробнее: https://render.com/docs/custom-domains

---

### Q: Как увеличить лимит загрузок/памяти?
**A:**
1. На странице сервиса нажми "Edit"
2. Измени план на более высокий
3. Можешь выбрать больше CPU/памяти
4. Нажми "Update"

Render автоматически масштабирует сервис.

---

## 🔧 Решение Проблем

### 🔴 БД не подключается

**Ошибка в логах:**
```
❌ Database connection failed: connect ECONNREFUSED ::1:5432
```

**Решение:**

1. **Проверь что БД создана:**
   ```
   1. Перейди на https://dashboard.render.com
   2. Ищи свою БД в списке сервисов
   3. Статус должен быть "Available" (зеленый)
   ```

2. **Проверь переменные окружения:**
   ```
   На странице backend:
   - Environment
   - Убедись что все DB_* переменные установлены
   - Нет ли опечаток?
   - Пароль скопирован полностью?
   ```

3. **Используешь ли ты Internal Database URL?**
   ```
   ✅ ПРАВИЛЬНО: postgres://postgres:pass@dpg-xyz.render.com:5432/...
   ❌ НЕПРАВИЛЬНО: postgresql://postgres:pass@ec2-xyz.amazonaws.com:5432/...
   
   Должен быть dpg-xyz.render.com, не amazonaws.com!
   ```

4. **Проверь регионы:**
   ```
   Backend и БД должны быть в ОДНОМ регионе!
   Если diferentes регионы - замедление и возможно timeout.
   ```

---

### 🔴 "timeout" при подключении

**Ошибка в логах:**
```
❌ Database connection failed: Connection timeout
```

**Решение:**

1. **Используй Internal Database URL** (не External)

2. **Убедись что регионы совпадают:**
   ```
   Backend region и Database region должны быть одинаковыми
   Если нет - перемести один из них
   ```

3. **Может быть БД еще инициализируется:**
   ```
   - Создание БД займет 3-5 минут
   - Дождись статуса "Available"
   - Потом перезагрузи backend
   ```

4. **Проверь пароль:**
   ```
   DB_PASSWORD не должен содержать:
   - Пробелы
   - Специальные символы (кроме буквквально перечисленного)
   - Кавычки
   ```

---

### 🔴 Frontend открывается, но API не работает

**Ошибка в браузере:**
```
GET https://photo-hosting.onrender.com/api/posts 404
Cross-Origin Request Blocked
```

**Решение:**

1. **Проверь FRONTEND_URL в переменных:**
   ```
   Должна быть: https://photo-hosting.onrender.com
   Без слеша в конце!
   ```

2. **Проверь API_URL в переменных:**
   ```
   Должна быть: https://photo-hosting.onrender.com/api
   Без слеша в конце!
   ```

3. **Перезагрузи backend:**
   ```
   Нажми на backend сервис
   Нажми кнопку "Manual Deploy" или подожди автоматическую перезагрузку
   ```

4. **Проверь фронтенд конфиг:**
   ```
   В frontend/.env.production проверь:
   REACT_APP_API_URL=https://photo-hosting.onrender.com/api
   ```

---

### 🔴 Сайт очень медленно загружается

**Причины:**
1. Free план имеет ограниченные ресурсы
2. БД на Free плане может быть медленной
3. Первый запрос к спящему сервису может быть медленным

**Решение:**

1. **Upgrade план:**
   ```
   Backend: Upgrade с Free на Starter
   Database: Upgrade с Free на Starter
   ```

2. **Включи caching:**
   ```
   Это может быть добавлено в backend коде
   ```

3. **Оптимизируй изображения:**
   ```
   Убедись что фото не больше 5-10 МБ
   Используй формат WebP или JPEG, не PNG
   ```

---

### 🔴 "Out of Memory" ошибка

**Ошибка в логах:**
```
JavaScript heap out of memory
```

**Решение:**

1. **Upgrade backend план:**
   ```
   На странице backend сервиса:
   Edit → выбери больше памяти (512 MB или 1 GB)
   ```

2. **Проверь утечки памяти в коде:**
   ```
   Может быть процесс не закрывается правильно
   Смотри логи для деталей
   ```

3. **Перезагрузи сервис:**
   ```
   Нажми кнопку "Reboot" на странице сервиса
   ```

---

### 🔴 "Connection refused" после добавления переменных

**Это нормально!**

Обычно это значит:
1. БД еще не готова
2. Переменные еще не применились
3. Сервис еще перезагружается

**Решение:**
```
Подожди 1-2 минуты
Потом обнови страницу логов (F5)
Должно начать работать
```

---

### 🔴 Все переменные правильные, но все равно ошибка

**Решение - ядерная опция:**

1. **Заново создай БД:**
   ```
   1. На странице текущей БД нажми "Delete"
   2. Создай новую БД (2 минуты)
   3. Скопируй новый Connection String
   4. Обнови переменные на backend
   5. Перезагрузи backend
   ```

2. **Заново deploy backend:**
   ```
   На странице backend:
   Нажми "Manual Deploy"
   Жди полной перезагрузки
   ```

---

## 📊 Мониторинг и Статистика

### Где смотреть использование ресурсов?

1. **На странице сервиса:**
   ```
   Metrics → CPU, Memory, Network графики
   Logs → все логи
   Events → история событий
   ```

2. **На странице БД:**
   ```
   Insights → использование памяти
   Query Stats → медленные запросы
   ```

---

### Как узнать сколько данных в БД?

**Через psql (если у тебя PostgreSQL клиент):**
```bash
psql postgres://user:pass@host:5432/photo_hosting
SELECT pg_database.datname, 
       pg_size_pretty(pg_database_size(pg_database.datname)) 
FROM pg_database 
WHERE datname = 'photo_hosting';
```

Или просто смотри в Render Dashboard → Database → Insights

---

## 💡 Полезные Советы

### Совет 1: Используй Health Check
```
curl https://photo-hosting.onrender.com/health
```

Если возвращает JSON с "OK" - backend живой ✅

### Совет 2: Смотри логи в реальном времени
```
На странице backend:
Logs → refresh каждые 10 секунд
Нажми на лог строку для полного текста
```

### Совет 3: Используй Database URL для подключения через инструменты
```
DBeaver / DataGrip / pgAdmin могут подключиться используя External URL
Но для backend используй Internal URL!
```

### Совет 4: Регулярно проверяй логи
```
Даже если всё работает - смотри логи
Там могут быть предупреждения или ошибки
```

---

## 📞 Если Ничего Не Помогает

1. **Проверь официальную документацию:**
   https://render.com/docs/databases

2. **Посмотри примеры настройки:**
   https://render.com/docs/databases#connecting-to-your-database

3. **Напиши в Render support:**
   https://render.com/docs/support

4. **Посмотри наши гайды:**
   - [RENDER_SETUP.md](./RENDER_SETUP.md)
   - [RENDER_VISUAL_GUIDE.md](./RENDER_VISUAL_GUIDE.md)

---

**Последнее обновление:** 2025-11-29  
**Проверено на:** Render.com Node.js 18
